package main.java.ch2.r16;

/**
 * 레시피 2-16 : 애스펙트 포인트컷 재사용하기
 *
 * 포인트컷 표현식을 여러 번 되풀이해서 쓸 경우, 어드바이스 애너테이션에 직접 써넣는 것보다 재사용할 방법이 필요하다.
 *
 * ->
 * @Pointcut을 이용하면, 포인트컷만 따로 정의해 여러 어드바이스에서 재사용할 수 있다.
 *
 * 애스펙트에서 포인트컷은 @Pointcut을 붙인 단순 메서드로 선언할 수 있다.
 *
 * 포인트컷과 애플리케이션 로직이 뒤섞이는 것은 바람직하지 않으니, 메서드 바디는 보통 비워두고 포인트컷의 가시성은 메서드의 수정자(접근 제한자)로 조정한다.
 *
 * 이렇게 선언한 포인트컷은 다른 어드바이스가 메서드명으로 참조한다.
 *
 * */


public class Main {

}
