package main.java.ch2.r17;

/**
 * 레시피2-17 : AspectJ 포인트컷 표현식 작성하기
 *
 * 공통 관심사는 프로그램이 실행되는 지점, 즉 여러 조인포인트에 걸쳐 분포한다.
 * 조인포인트는 천차만별이므로 뭔가 더 강력한 표현식 언어로 매치할 방법이 필요하다.
 *
 * ->
 * ApsectJ는 다양한 종류의 조인포인트를 매치할 수 있는 강력한 표현식 언어를 제공한다.
 * 하지만 스프링 AOP가 지원하는 조인포인트 대상은 IoC 컨테이너 안에 선언된 빈에 국한된다.
 *
 * - 메시지 시그니처 패턴
 *  : 시그니처 기준으로 여러 메서드 매치. 수정자, 반환형, 인수 개수, 인수 타입 등 다양하게 지정 가능
 * -
 *
 *
 *
 * */

public class Main {
}
